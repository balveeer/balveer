import React from 'react';

const Title = ({title}) => {
    return (
        <h2 className="py-2 text-5xl text-center text-black dark:text-white rubik-mono-one-regular">{title}</h2>
    );
};

export default Title;